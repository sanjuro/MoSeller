Invoice for Order Number: <%= @order.number %>
===============================================

Invoice Number: <%= @invoice.id %>
Invoice Date: <%= @invoice.created_at %>
 

    <% @invoice.invoice_items.each do |item| %>
        <%=item.description %>
        <%= number_to_currency(item.total, :unit => "R") %>
    <% end %>
    
    SubTotal: <%= number_to_currency(@invoice.sub_total, :unit => "R") %>
    
    Total: <%= number_to_currency(@invoice.total, :unit => "R") %>
 
Thanks for buying and have a great day!