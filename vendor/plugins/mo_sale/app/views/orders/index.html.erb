<h2>Orders</h2>
 
<%= will_paginate %> 
 
<table> 
<% @orders.each do |order| %>
<thead>
	<th width="200px">Order Number</th>
	<th width="200px" text-align="right">Price</th>
	<th width="200px">Order Date</th>
</thead>
<tbody>
  <tr>
    <td><%= link_to order.number , order %></td>
    <td><%= order.customer_total %></td>
    <td><%= order.created_at %></td>
  </tr>
</tbody>
<% end %>
</table>
 
<br />

<%= will_paginate %>